apiVersion: v1
kind: Service
metadata:
  name: {{ include "hancock.fullname" . }}
  labels:
    {{- include "hancock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hancock.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
