apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hancock.fullname" . }}-config
  labels:
    {{- include "hancock.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
